"use client";import {createBrowserClient} from "@supabase/ssr";import {useState} from "react";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("Authentication is not configured.");return}const {error}=await createBrowserClient(url,key).auth.signInWithPassword({email,password});if(error)setMessage("Unable to sign in.");else location.href="/admin"}return <main className="login"><form onSubmit={submit}><a className="brand" href="/">CLICK <span>NUVIA</span></a><p className="eyebrow">OPERATIONS PORTAL</p><h1>Welcome back</h1><label>Work email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{message&&<p className="error">{message}</p>}<button className="next" type="submit">Sign in</button></form></main>}
