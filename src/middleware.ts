import { createServerClient,type CookieOptions } from "@supabase/ssr";
import { NextResponse,type NextRequest } from "next/server";
export async function middleware(req:NextRequest){let res=NextResponse.next({request:req});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return NextResponse.redirect(new URL("/admin/login?error=configuration",req.url));const supabase=createServerClient(url,key,{cookies:{getAll:()=>req.cookies.getAll(),setAll(cookies:{name:string;value:string;options:CookieOptions}[]){cookies.forEach(({name,value})=>req.cookies.set(name,value));res=NextResponse.next({request:req});cookies.forEach(({name,value,options})=>res.cookies.set(name,value,options))}}});const {data:{user}}=await supabase.auth.getUser();if(req.nextUrl.pathname==="/admin/login")return user?NextResponse.redirect(new URL("/admin",req.url)):res;const domains=(process.env.ADMIN_EMAIL_DOMAINS||"").split(",").map(x=>x.trim().toLowerCase()).filter(Boolean),domain=user?.email?.split("@")[1]?.toLowerCase();if(!user||!domain||!domains.includes(domain))return NextResponse.redirect(new URL("/admin/login",req.url));return res}
export const config={matcher:["/admin/:path*"]};
